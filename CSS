.error {
  color: red;
  font-size: 0.8em;
}

input[type="submit"] {
  padding: 0.5rem 1rem;
  background-color: #333;
  color: white;
  border: none;
  cursor: pointer;
}

input[type="submit"]:hover {
  background-color: #555;
}
